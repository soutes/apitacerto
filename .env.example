# Copie para .env e preencha. .env nunca vai para o git.

# Fonte secundaria de dados (scripts/ingest.py), opcional
API_FOOTBALL_KEY=

# Docker Compose (compose.yaml). Sem isso, valores de desenvolvimento local.
POSTGRES_USER=apitacerto
POSTGRES_PASSWORD=troque-esta-senha
APP_PORT=8000
